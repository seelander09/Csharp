using System;

namespace lowerCase
{
    class Program
    {
        static void Main(string[] args)
        {
            string line = "Write a C# Program to display the following pattern using alphabet.";
            Console.WriteLine(line.ToLower());

            string upper = "ALL CAPS MAKES PEOPLE THINK YOUR YELLING";
            Console.WriteLine(upper.ToLower());
        }
    }
}
